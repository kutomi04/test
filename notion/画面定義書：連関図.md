# 画面定義書：連関図（画面遷移・データ連携）テンプレ

<aside>
💡

このページは、画面定義書に付ける「連関図」を作るためのテンプレです。

下のMermaidを、あなたの画面名・データ（テーブル/ID/主キー）に合わせて書き換えてください。

</aside>

## 1) まず決める（最小セット）

- 画面一覧（例：ログイン、トップ、一覧、詳細、編集）
- 画面間のつながり：遷移（ボタン/リンク）
- 画面が扱うデータ：参照/登録/更新/削除（CRUD）

## 2) 画面遷移の連関図（ユーザー操作）

```mermaid
flowchart LR
	Login[ログイン画面] --> Home[メニュー画面]
	style Login fill:#98fb98,stroke:#2e7d32,stroke-width:2px
	style Home fill:#d7ccc8,stroke:#5d4037,stroke-width:2px
	Home --> List[保育園一覧]
	style List fill:#e1bee7,stroke:#7b1fa2,stroke-width:2px
	List --> new[保育園新規登録]
	List --> change[保育園変更登録]
	List -- 削除ボタン --> delete{削除処理}
	delete -.-Note[備考:保育園一覧に削除ボタン。 実行前に確認ダイアログを表示]
	style delete fill:#f96,stroke:#333,stroke-width:2px
	Home --> List2[商品一覧]
	style List2 fill:#e1bee7,stroke:#7b1fa2,stroke-width:2px
	List2 --> new2[商品新規登録]
	List2 --> change2[商品変更登録]
	List2 -- 削除ボタン --> delete2{削除処理}
	style delete2 fill:#f96,stroke:#333,stroke-width:2px
	delete2 -.-Note2[備考:商品一覧に削除ボタン。 実行前に確認ダイアログを表示]
	Home --> List3[出荷予定一覧]
	style List3 fill:#e1bee7,stroke:#7b1fa2,stroke-width:2px
	List3 --> new3[出荷新規登録]
	List3 --> change3[出荷変更登録]
	List3 --> history3[出荷履歴]
	List3 -- 削除ボタン --> delete3{削除処理}
	delete3 -.-Note3[備考:出荷一覧に削除ボタン。 実行前に確認ダイアログを表示]
	style delete3 fill:#f96,stroke:#333,stroke-width:2px
	
```

## 3) 画面 × データ（CRUD）の連関図（システム連携）

```mermaid
flowchart LR
	subgraph Screens[画面]
		ListS[一覧画面]
		DetailS[詳細画面]
		EditS[編集画面]
	end

	subgraph Data[データ/テーブル]
		UserT[(User)]
		OrderT[(Order)]
	end

	ListS -- R:検索/一覧取得 --> OrderT
	DetailS -- R:1件取得 --> OrderT
	EditS -- U:更新 --> OrderT
	DetailS -- R:参照 --> UserT
```

## 4) 画面間の連携（パラメータ/ID受け渡し）

- 一覧 → 詳細：`orderId`
- 詳細 → 編集：`orderId`
- 編集 → 詳細：更新後に再取得（`orderId`）

## 5) 書き方ルール（おすすめ）

- 画面名は「業務名 + 画面種別」（例：受注一覧、受注詳細）
- 線にラベルを付ける（例：`R`/`C`/`U`/`D`、または「検索」「登録」など）
- データはテーブル名/主要キー（例：`Order(orderId)`）まで書く

## 6) ヒアリング用の記入欄（ここを埋めると図が完成します）

- 対象システム/機能：
- 画面一覧：
- 主要テーブル（またはAPI）：
- 画面→データの操作（CRUD）：
- 画面遷移（起点/条件/例外）：
- 受け渡すパラメータ：